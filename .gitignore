# Local configuration and credentials
.env
.env.*
!.env.example

# WhatsApp and local database state
session/
session_*/
data/session.db
data/session.db-*
data/*.sqlite
data/*.sqlite-*

# Runtime backups
_backups/
# NOTE: eplugins/ is tracked (Yato plugins source). Do NOT ignore it.

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Temporary / cache
tmp/
temp/
.cache/
*.log
.DS_Store

# IDE / editor
.vscode/
.idea/
*.swp
*.swo

# Build output
dist/
build/

# Web dashboard runtime data (لا تُنشر — بيانات مستخدمين حقيقية)
data/web_users.json
data/web_sessions.json
data/site_settings.json

# إعدادات الجلسات الفردية (تُنشأ عند التشغيل)
data/config_*.json

# جلسات واتساب (بيانات اعتماد — لا تُنشر أبداً)
data/sessions/
data/paired_users.json
